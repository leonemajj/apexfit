import 'package:flutter/material.dart';

class Palette {
  static const bg = Color(0xFF0B0E14);        // deep matte
  static const surface = Color(0xFF101622);   // cards
  static const surface2 = Color(0xFF151C2B);  // elevated surfaces
  static const stroke = Color(0xFF202A3D);    // subtle borders
  static const text = Color(0xFFF2F5FF);
  static const textMuted = Color(0xFF9AA6BF);

  static const accent = Color(0xFF7C5CFF);    // premium purple
  static const accent2 = Color(0xFF23D2A1);   // emerald
  static const danger = Color(0xFFFF4D6D);
  static const warning = Color(0xFFFFB020);
}
